# 校园工作台 · 嵌入企业微信工作台部署说明

## 方案说明

企业微信工作台支持两类自建应用：**小程序** 和 **网页（H5）**。

- 真·小程序需要：注册小程序主体、微信开发者工具开发、审核发布，流程约 1~2 周。
- **网页型应用（本方案）**：只需一个 HTTPS 地址，配置到企业微信后台即可出现在员工工作台，体验与小程序基本一致（可全屏、无浏览器地址栏）。

本应用为多模块 H5 + Vercel Functions + Vercel Blob，同时保留可独立部署的 `server.js`：

- 工作台首页 → 统一进入作息时间表和校历。
- 作息时间表 → 查看当前时段、今日进度和本周安排，管理员可导入、编辑并发布。
- 校历 → 公开显示“开发中”状态，管理员登录后可查看、添加、删除并发布。

## 一、部署（获取 HTTPS 地址）

> **推荐方式 V：Vercel**。自带 HTTPS、持久化存储和「保存并发布」，无需自备服务器。

**方式 V：Vercel Functions + Vercel Blob（推荐）**

1. 在 Vercel 导入 GitHub 仓库，Framework Preset 选择 `Other`。
2. 为项目创建 Private Blob Store，并连接 Production、Preview、Development 环境。
3. 新建 Secret 环境变量 `ADMIN_PASS`，作为在线管理密码；管理员账号默认是 `admin`，也可用 `ADMIN_USER` 修改。需要同步企业微信智能表格时，再新增一条独立的 `CALENDAR_SYNC_SECRET`。
4. 部署后，`api/*.mjs` 会自动成为 Vercel Functions，作息与校历分别保存在私有 Blob 中。

Vercel 环境下不会上传 `server.js`；`.vercelignore` 已将它排除，避免被误别为单体 Node 入口。

工作台入口为 `https://ssfyx.site/`；原作息地址 `https://ssfyx.site/work-schedule/` 保持可用，校历地址为 `https://ssfyx.site/school-calendar/`。

**方式 D：Node 极简后端（推荐，全员即时同步）**

1. 服务器安装 Node.js 16+（`node -v` 验证）。
2. 上传整个项目目录，需包含 `index.html`、`modules/`、`assets/`、`api/`、`data/`、`server.js` 和 `package.json`。
3. 启动（可用 pm2 保活）：
   ```bash
   cd /opt/schedule
   npm run start:local  # 或 node server.js
   # 保活：npm i -g pm2 && pm2 start server.js --name schedule && pm2 save
   ```
4. Nginx 配置 HTTPS 反代：
   ```nginx
   server {
     listen 443 ssl;
     server_name schedule.yourcompany.com;
     ssl_certificate     /path/to/fullchain.pem;
     ssl_certificate_key /path/to/privkey.pem;
     location / { proxy_pass http://127.0.0.1:3000; }
   }
   ```
   （端口可通过环境变量 `PORT` 修改）
5. 打开 `https://schedule.yourcompany.com/` → 右上角 ⚙ → 修改作息 → **「保存并发布」** → 全员即时生效。

后端接口（同域部署无需跨域处理）：

| 接口 | 说明 |
|---|---|
| `GET /api/health` | 健康检查（前端自动探测后端是否可用） |
| `GET /api/schedule` | 返回当前作息配置（旧 `/api/config` 仍兼容） |
| `POST /api/schedule` | 保存发布作息，body：`{ adminPass, config }` |
| `GET /api/calendar` | 管理员令牌验证后返回当前校历配置 |
| `POST /api/calendar` | 管理员令牌验证后保存发布校历 |
| `POST /api/calendar-sync` | 使用独立同步密钥接收智能表格校历数据 |
| `GET /api/module-mappings` | 管理员读取网页与企业微信表格映射 |
| `POST /api/module-mappings` | 管理员保存草稿或发布映射配置 |

配置分别存储在 `data/config.json` 和 `data/calendar.json`（建议纳入定期备份）。管理密码必须通过环境变量 `ADMIN_PASS` 设置，不写入数据文件。

部署后可通过 `https://你的域名/admin/mappings/` 进入数据映射中心。映射中心和校历后台共用管理员账号及登录令牌；Vercel 环境还需要正确连接 Private Blob Store 才能保存映射。

**方式 A：内部服务器 / Nginx（纯静态，无后端）**

1. 将 `index.html` 上传到服务器（如 `/var/www/schedule/`）。
2. Nginx 配置示例：
   ```nginx
   server {
     listen 443 ssl;
     server_name schedule.yourcompany.com;
     ssl_certificate     /path/to/fullchain.pem;
     ssl_certificate_key /path/to/privkey.pem;
     location / { root /var/www/schedule; index index.html; }
   }
   ```
   此模式下后台无「保存并发布」，需用「导出 config.json」手动上传同步。

**方式 B：静态托管平台（腾讯云 COS / EdgeOne Pages 等）**
上传 `index.html`，开启 HTTPS 访问即可（同方式 A，无后端）。

**方式 C：内网穿透临时测试**
用 frp、花生壳等工具临时生成 HTTPS 地址做内部验证。

> 注意：企业微信网页应用**必须使用 HTTPS**，且域名需通过企业微信的「可信域名」校验。

## 二、在企业微信后台创建应用

1. 管理员登录 [企业微信管理后台](https://work.weixin.qq.com) → 「应用管理」→「应用」→ **「创建应用」**。
2. 填写应用名称（如「作息时间表」）、上传 Logo、选择可见范围。
3. 创建完成后，在应用详情页找到 **「网页授权及JS-SDK」** → 设置可信域名（如 `schedule.yourcompany.com`），按提示完成域名归属验证（下载 txt 文件放到网站根目录）。
4. 在应用详情页「工作台应用主页」→ 设置为：
   ```
   https://schedule.yourcompany.com/index.html
   ```

5. 保存后，员工在手机企业微信「工作台」即可看到该应用图标，点开即为全屏 H5。

## 三、后台更换作息（推荐方式，无需改代码）

1. 管理员打开页面，点击右上角 **⚙** 输入管理密码进入后台。Vercel 使用 `ADMIN_PASS`；纯静态/本机模式默认为 `8888`。
2. 修改方式任选：
   - **直接编辑**：改公司名称、勾选工作日、增删改作息时段、编辑温馨提示；
   - **导入文件自动生成**：点「选择 CSV / Excel 文件」，选入作息表文件后自动生成全部时段；
   - 后台内置「下载模板 (CSV)」和「下载模板 (Excel)」按钮，可先下载模板填写再导入。
3. 保存与同步：
   - **保存并发布（全员生效）**：后端已部署时（后台顶部显示绿色「后端：已连接」），点一下即写入服务器，全员打开页面即时看到新作息；
   - **仅本机保存**：仅当前设备生效（本地演示/测试用）；
   - **导出 config.json**：无后端时手动同步——上传到服务器 `index.html` 同目录覆盖，全员重新打开后生效。

### 配置生效优先级（展示端自动按序读取）

```
URL 参数 ?config=xxx.json  →  后端 /api/schedule  →  服务器 /config.json  →  本机保存(localStorage)  →  页面内置默认值
```

### 导入模板格式

| 列    | 说明                                    |
| ---- | ------------------------------------- |
| 名称   | 时段名称，如「上午工作」                          |
| 开始时间 | HH:MM，如 09:00                         |
| 结束时间 | 可留空（表示打卡等单点事件）                        |
| 类型   | 工作、休息、关键节点、学生入校、上课、大课间活动、眼保健操时间、午餐时间、卫生检查、校园广播、午休时间、社团 |
| 说明   | 选填                                    |

Excel 中可另加「设置」Sheet，两列（键 / 值），支持：`公司名称`、`周一`~`周日`（1 上班 / 0 休息）、`温馨提示`（可多行）。

## 四、修改默认配置（改代码方式）

打开 `modules/work-schedule/index.html`，找到 `DEFAULT_CONFIG` 对象：

- `company`：公司名称
- `schedule`：默认作息条目
- `workdays`：每周哪几天上班（数组下标 0=周日 … 6=周六）
- `tips`：温馨提示文案列表

> 后端模式下密码只以服务器环境变量 `ADMIN_PASS` 为准。

## 五、同步企业微信智能表格

企业微信授权保留在管理员电脑，不部署到 Vercel。网站只接收已经转换和校验过的校历 JSON。

1. 安装并授权 `wecom-cli`。
2. 复制 `config/wecom-calendar.example.json` 为 `config/wecom-calendar.local.json`。
3. 在本地配置中填写智能表格链接、需要同步的子表名称，以及日期、事件、重要程度等字段名称。
4. 在 Vercel 和执行同步的电脑上配置相同的 `CALENDAR_SYNC_SECRET`。该值应使用单独生成的高强度随机字符串，不要与管理员密码共用。在 macOS 上也可以把密钥存入系统钥匙串，服务名称使用 `ssfyx-calendar-sync`，同步程序会自动读取。
5. 先进行只读演练：

   ```bash
   npm run sync:calendar -- --dry-run
   ```

6. 确认结果后发布：

   ```bash
   CALENDAR_SYNC_SECRET='你的同步密钥' npm run sync:calendar
   ```

每次同步只访问配置中指定的智能表格文档和子表，并用子表中的当前有效记录替换网站校历数据。若后期换表，只需修改本地配置中的 `source.url`、`source.sheet` 和 `source.fields`。建议先执行 `--dry-run`，确认字段与数据映射无误后再正式同步。

### 本地工作台

使用可视化本地工作台读取真实子表和字段：

```bash
npm run start:console
```

浏览器打开 `http://127.0.0.1:3100/local-console/`。首次使用需要设置至少 8 个字符的本地密码，之后登录才能访问企业微信状态、子表、字段、数据预览和保存接口。密码只以加盐哈希形式保存在本机 `data/local-console-auth.json`，不会上传。

本地工作台只监听当前电脑，能够检查企业微信授权、读取用户填写链接对应的子表和字段、预览最多 8 行数据并保存本地映射草稿。当前版本尚未把通用模块数据发布到 Vercel；校历正式同步仍使用上面的 `npm run sync:calendar`。

## 六、可选增强（后续迭代）

| 需求               | 做法                                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------- |
| 展示轮班/多班组作息       | config 改为按班组多套 schedule，展示端加 Tab 切换                                                          |
| 接入企业微信身份（显示员工姓名） | 引入 [企业微信 JS-SDK](https://developer.work.weixin.qq.com/document/path/90514)，用 OAuth 获取 userid |
| 打卡提醒推送           | 后台定时任务 + 企业微信「应用消息」API 推送                                                                    |
| 多人/多部门分级管理       | server.js 增加多账号（存于 data/admin.json），后台按部门发布                                        |

如果后续确需打包为真·小程序，可将本页面逻辑迁移到 WXML/JS，主体 UI 和数据结构可以直接复用。
