{
  "name": "schedule-server",
  "version": "1.0.0",
  "private": true,
  "description": "作息时间表（Vercel Functions + Vercel Blob）",
  "scripts": {
    "start:local": "node server.js",
    "check": "node --check server.js && node --check api/health.mjs && node --check api/auth.mjs && node --check api/config.mjs"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.0",
    "xlsx": "^0.18.5"
  }
}
